fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx[socks]>=0.27.0
keyring>=25.2.0  # legacy-migration reader ONLY (E5 moved secrets into the vault DB)
pydantic>=2.7.0
# Pulled in by fastapi, pinned here anyway. Starlette 0.8.3 to 1.0.0 rebuild
# request.url from the Host header without validating it (CVE-2026-48710),
# which is the exact value vault_gate and the CSRF shield make decisions on.
# The installed version is well past it; without a floor, an offline install
# from an old wheel cache could silently land inside that range.
starlette>=1.0.1
pillow>=10.0
python-multipart>=0.0.9
# Declared here because thirteen shipped runtime modules import anyio.to_thread
# directly (openrouter, proxy_health, notebook_worker, tts/stream_hook and nine
# routers). It arrived in the lock as an httpx/starlette transitive, and it was
# named only in requirements-dev.txt, whose comment scopes it to the test
# plugin - so if either upstream ever drops it, the lock regenerates without it
# and the exe fails at import while the dev-file comment makes the absence look
# deliberate. Declaring it is what stops a transitive from silently becoming
# load-bearing. The resolved version does not move: 4.13.0 is already pinned
# and hashed in requirements.lock.txt.
anyio>=4.0
# Capped below 0.6: this wheel bundles the SQLCipher native lib, and a major
# cipher-default bump could stop opening existing raw-key vaults on pip -U.
sqlcipher3-wheels>=0.5.7,<0.6
# Desktop packaging: native window (run_app.py) + one-file exe.
pywebview>=6.2  # v1.1 H17: WebView2 AllowExternalDrop default TRUE (DnD reaches the DOM)
pyinstaller>=6.0
